name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Trigger on push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository (main branch)
      - name: Checkout main branch
        uses: actions/checkout@v3

      # Step 2: Checkout gh-pages branch (create if it doesn't exist)
      - name: Checkout gh-pages branch
        run: |
          git fetch --all
          git checkout gh-pages || git checkout --orphan gh-pages

      # Step 3: Clean up docs folder
      - name: Clean docs folder
        run: |
          rm -rf docs/*  # Remove all content from docs folder

      # Step 4: Copy the contents of the output folder to the docs folder
      - name: Copy Output to docs folder
        run: |
          cp -r output/* docs/  # Copy all files from Output to docs folder

      # Step 5: Commit and push changes to gh-pages branch
      - name: Commit and push changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add docs/
          git commit -m "Deploy website to docs folder"
          git push origin gh-pages --force
